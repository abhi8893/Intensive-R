# Returns a list with start and end indices of the
# runs greater than atleast k in a binary vector
# Runs should NOT be contained within each other
consec.runs <- function(v, k){
  start = end <- vector(mode="numeric", length=length(v))
  s = e <- 1 # start and end
  run.len <- 0
  last.val <- v[1]
}


